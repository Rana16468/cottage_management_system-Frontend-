import React from "react";

const ProductDetailsCard = ({ productDetails, isLoading, error }) => {
  console.log(productDetails);
  console.log(isLoading);
  console.log(error);
  return (
    <>
      <div className="w-full px-4 py-2  lg:w-full"></div>
    </>
  );
};

export default ProductDetailsCard;
